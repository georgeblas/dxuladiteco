import AdminPanel from "@/components/AdminPanel";

export const metadata = { title: "Panel del negocio — Dxuladiteco", robots: { index: false } };

export default function AdminPage() {
  return <AdminPanel />;
}
